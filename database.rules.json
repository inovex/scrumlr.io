{
  "rules": {
    ".read": false,
    ".write": false,
    "boards": {
      "$boardId": {
        ".read": "!root.child('boards').hasChild($boardId) || data.child('private/config/adminUid').val() === auth.uid",
        ".write": "!root.child('boards').hasChild($boardId) || data.child('private/config/adminUid').val() === auth.uid",

        "public": {
          "config": {
            ".read": true
          },
          "applicants": {
            "$uid": {
              ".read": "$uid === auth.uid",
              ".write": "$uid === auth.uid"
            }
          },
          "accessAuthorized": {
            "$uid": {
              ".read": "$uid === auth.uid"
            }
          }
        },

        "private": {
          ".read": "data.child('members').hasChild(auth.uid)",
          "cards": {
            "$cardId": {
              ".write": "!root.child('boards/'+$boardId+'/private/cards').hasChild($cardId)",
              "userVotes": {
                "$uid": {
                  ".write": "$uid === auth.uid"
                }
              },
              "votes": {
                ".write": "null !== auth.uid"
              }
            }
          },
          "members": {
            "$uid": {
              ".write": "$uid === auth.uid && (root.child('boards/'+$boardId+'/public/secure').val() === false || root.child('boards/'+$boardId+'/public/accessAuthorized').hasChild(auth.uid))"
            }
          },
          "online": {
            "$uid": {
              ".write": "$uid === auth.uid && (root.child('boards/'+$boardId+'/public/secure').val() === false || root.child('boards/'+$boardId+'/public/accessAuthorized').hasChild(auth.uid))"
            }
          }
        }
      }
    }
  }
}