import {render} from "testUtils";
import {Avatar} from "../Avatar";

// with the new avataaars library, testing for specifics (e.g. has a beard) would be very cumbersome, so we leave that out
describe("Avatar", () => {
  it("properties will be generated by the seed", () => {
    const {container} = render(<Avatar seed="0" />);
    expect(container).toMatchSnapshot();
  });

  it("seed will define an accent color class", () => {
    const {container} = render(<Avatar seed="0" />);
    expect(container.firstChild).toHaveClass("accent-color__online-orange");
  });
});
