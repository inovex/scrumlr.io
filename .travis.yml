language: node_js
node_js:
  - "node"
cache: yarn

install:
  - npm install -g codecov
  - yarn install

script:
  - yarn lint
  - yarn css:build
  - yarn test -- --coverage

after_success:
  - codecov