apiVersion: v1
kind: Secret
metadata:
  name: {{ include "scrumlr-fullname" .Values.server }}
  labels: 
    {{- include "selectorLabels" .Values.server | nindent 6 }}
    {{- include "labels" . | nindent 6 }}
{{- if .Values.server.secrets }}
data:
{{ toYaml .Values.server.secrets | indent 2 }}
{{- end}}